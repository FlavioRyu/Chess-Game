{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "cppbuild",
			"command": "g++.exe",
			"label": "g++.exe build active file",
			"args": [
				"-g",
				"${fileDirname}\\**.cpp",
				//"${fileDirname}\\**.h",
				"-o",
				"${fileDirname}\\${fileBasenameNoExtension}.exe",
			],			
			"options": {
				"cwd": "${workspaceFolder}"
			},
			"problemMatcher": [
				"$msCompile"
			],
			"group": "build",
			"detail": "compiler: cl.exe"
		}
	]
}